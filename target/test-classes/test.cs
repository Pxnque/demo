using System;

class Program {
    static void Main(string[] args) {
        int count = 15;
        float ratio = 2.5f;
        bool flag = true;
        string message = "Custom language active";
        char grade = 'A';
        Console.WriteLine(message);
        if (flag == true)
        {
            Console.WriteLine("Flag is true");
        }
        Console.WriteLine("Ratio updated");
        Console.WriteLine(message);
        Console.WriteLine(count);
        Console.WriteLine(count);
        int c = 0;
        for (; c <= 10; c++)
        {
            Console.WriteLine("Looping...");
            Console.WriteLine(c);
        }
        int total = 2;
        Console.WriteLine(total);
        if (total < 0)
        {
            Console.WriteLine("All checks passed");
        }
        int w = 0;
        while (w < 10)
        {
            Console.WriteLine(w);
        }
    }
}
